# 2026-09-16 · docs/23 二期（P3 部门作为申请主体）+ 三期（P4 知会完整版）落地

## 编排方式
本轮走「软件开发团队」SOP：产品经理（许清楚）出增量 PRD → 架构师（高见远）出增量设计 + 任务分解 → 工程师（寇豆码）实现 → QA（严过关）独立验证。团队 `software-aioa-p23`。
交付物：`docs/25`（增量 PRD）、`docs/26`（增量架构设计 + 类图/序列图 `.mermaid`）、`docs/27`（二期三期验收报告）。

## 迁移
- `V43__dept_applicant_and_cc_read.sql`（DDL）：
  `approval_order.applicant_type VARCHAR(16) NOT NULL DEFAULT 'USER'`、`approval_order.applicant_department_id BIGINT NULL`、
  `permission_grant.applicant_type VARCHAR(16) NOT NULL DEFAULT 'USER'`（E-08 分账必需）、
  `approval_task.cc_read_at DATETIME(6) NULL`（NULL=未读）+ 索引。
- `V44__backfill_default_cc.sql`（回填）：四类业务租户默认流补默认 `cc`（`PERMISSION_GRANT`/`LEAVE`→`ORG_ADMIN`；`QUOTA_EXPAND`/`RESOURCE_OPEN`→`TENANT_ADMIN`），
  `JSON_CONTAINS_PATH` 守卫 + `institution_id=0 AND status='ACTIVE'`，**只补空、不覆盖**。结果类/发文类**不回填**（字面值未定）。
- 两条均 `success=1`。

## 二期（P3）实现要点
- `ApprovalFlowService.SubmitReq` 增 `applicantType`/`applicantDepartmentId`；`superiorLadder` 加 `DEPARTMENT` 分支：起点 = **`ORG_ADMIN`**（跳过 `DEPT_LEADER`，因提交人即负责人），**保留自审防护 + `levels` 截断**。
- 新增 `OrgGuard.requireDeptLeader(departmentId)`：**先机构归属（跨机构/平台 → 404）再职务（非 `duty_code='DEPT_PRINCIPAL'` → 403）**；回落 `leader_user_id`；**`job_title` 完全不参与权限判定**。附带 `ledDepartments(userId)`。
- `PermissionGrantService.ApplyReq` 增 `applyAsDepartment`/`departmentId`；幂等按 `(applicantType, dept|user, permissionCode)` **分账**；`toView`/`catalog` 增主体字段与 `canApplyAsDepartment`/`ledDepartments`（H5 部门开关**零新增端点**）。
- **授权仍发给提交人本人**（「部门共享权限」列为 Out of Scope）。
- 前端：H5「以部门名义申请」开关 + 部门选择 + 「部门申请」徽标；管理端审批中心主体徽标。

## 三期（P4 完整版）实现要点
- **引擎侧零改动**：`expandCcNodes` 在 `expandNodes` 内被**无条件**调用（与 `bizType` 无关），四重过滤已具备（按类型去重 / 不抄申请人 / 审批人不重复抄 / 解析不到跳过）→ C-01、C-08 天然满足，工作量在配置层。
- 新增 `POST /api/v1/workflow/cc/{taskId}/read`（幂等、微秒截断使 `readAt` 不倒退；他人任务 404；`APPROVE` 任务 `code!=0`）；
  `scope=cc` 每行带 `read`/`readAt` 且**已读仍可回查**；
  `summary` **保留 `cc` = 总条数**，**新增 `ccUnread`**。
- 新增管理端 `web/apps/shell/src/views/ApprovalFlowConfigView.vue` + 路由 `/approval-flows` + 菜单（此前只有 API 函数、无视图）。
- `ApprovalFlowProvisioner.DEFAULTS` 同步补 `cc`。

## 关键裁决（本轮）
1. **N-1 同意** 为 `permission_grant` 加 `applicant_type`（E-08 分账唯一可行解，默认 `'USER'` 零影响）。
2. **N-2 同意** 新建 `ApprovalFlowConfigView.vue`（C-02 是 P0，无视图无法验收）。
3. **N-4 同意** 只回填四类业务，成果/发文类不回填。
4. **R-1 底部「待办」角标不计入 CC 的站内通知**：`tab = 待审单数 + 未读通知数(剔除 refId ∈ 我的抄送单)`。
   依据 `docs/25 §4.1`「CC 仍不点亮待办角标；未读知会只体现在『抄送我的』Tab 内」——
   在 C-09（知会必推通知）前提下，两个分句只有这一种自洽实现。**改法仅前端 1 处**（`user-client/index.html:2087-2090`）。
   ⚠️ 触发过一次「断言 vs 规格」冲突：QA 新写的 `h5_v33_render.py A3-8.4` 把**旧公式**（含 CC 通知）当期望值 → 裁决后由 **QA** 更新断言（测试代码归 QA，工程师不越权）。
5. **R-2 越权语义**：跨机构/跨租户/平台 → 404；同机构非本部门正职 → 403；**均不落库**。

## 回归（QA 独立重跑，零回归）
- 新增：`e2e_v43_dept_applicant.py` **41/41** · `e2e_v43_cc_read.py` **40/40** · `h5_v33_render.py` **28/28**（15→25→28）
- 既有 18 套件全达基线：`e2e_full_system` 152/152 · `e2e_v41_duty_levels` 48/48 · `e2e_v39_applicant_superior` 49/49×2 · `e2e_v36_grant_expert_review` 64/64×2 · `e2e_v36_stats_clamp` 66/66 · `e2e_admin_personnel_scope` 57/57 · `e2e_v32_org_scope` 51/51 · `e2e_v33_roles` 41/41 · `e2e_p0a_worker_intake` 41/41 · `e2e_leave_flow_notify` 19/19 · `e2e_worker_permission` 16/16 · `e2e_login_tenant_name` 15/15 · `verify_v39_provisioner` 11/11 · `verify_config_effect` 11/11 · `admin_v39_todo_badge` 17/17×2 · `admin_v34_review_render` 8/8 · `check_org_structure_render` ALL PASS · `agent/tests` 42 passed。
- QA 的对抗性设计值得复用：越权四态 + **不落库行数比对**；**刻意改写 `job_title` 反证「job_title 不作权限依据」**（用后还原）；
  用**非 `PERMISSION_GRANT`** 业务真跑终态证「知会不阻塞」并回滚配额；`ccUnread` **先造未读再断言**（防假阳性）；
  A3-8.4 拆四条守卫（非空洞前置 / 精确公式 / 差分 / 「读掉知会后底部角标不变」反向），**期望值在 Python 侧独立推导、不复刻 H5 过滤**（防同源同错）。
- QA 主动列出 5 条「证伪失败的假设」（本来怀疑是 bug、验证后不是）：他部门正职可代发 / 幂等键污染 / 已读后条目消失 / `readAt` 漂移 / 平台账号错误码。

## 未做完
- `cc` 的 `SPECIFIC` 指定人（C-11，P1）；部门申请记录对部门成员可见（E-10，P2）；成果/发文类 `cc` 回填。
- 四期（`ApproverResolver` 策略族 `DEPT_DUTY`/`UNIT_DUTY`）、五期（并行/条件路由）仍未开工。
- 非本期发现：`e2e_v43_*` 之外，`e2e_v36_grant_expert_review` 等三个套件需连跑两次才稳（本仓惯例，已写进技能）。

## 环境备注
- `h5_v33_render` 的「AI 解读」曾假红：根因是**用 `agent/.venv` + `127.0.0.1` 起 agent** 导致请求体被丢弃 → 422；
  必须按 `start-all.sh` 口径（`envs/default` python + `--host 0.0.0.0`）重启 agent。**这条已写进技能。**
- 测试环境补装 `playwright 1.63.0`（用系统 Edge `channel="msedge"`）与 `bcrypt`，未改任何被测代码。
- git：已提交 `c0cde37`（`feat(V36-V44): 权限审批与组织关联改造…`，85 个文件：server/agent/user-client/web/docs/记忆；**排除** `scripts/` 与临时产物），并已推送成功。
- **推送通道重大修正**：`git -c http.sslVerify=false push github main`（HTTPS）**已不可用** ——
  报 `remote: Invalid username or token. Password authentication is not supported`；本机 GCM 无 token。
  旧记录「HTTPS 无需 PAT、失败根因是 Schannel `CRYPT_E_NO_REVOCATION_CHECK`」**已证伪**，不要再用。
  SSH 22 端口 `Connection refused`；**SSH over 443 可用**，但**必须显式 `-i` 指定私钥**
  （默认路径被 ssh 把中文 HOME 展开成乱码 `/c/Users/\301\365...` → 误报 `Permission denied (publickey)`）：
  ```bash
  GIT_SSH_COMMAND='ssh -i "C:/Users/刘尖尖/.ssh/id_rsa" -o IdentitiesOnly=yes \
    -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no' \
    git push ssh://git@ssh.github.com:443/liujiejie7089/AIOA.git main
  ```
  校验：本地 `c0cde377…` == 远端 `refs/heads/main` `c0cde377…`（**两侧一致**，已确认落库）。
- 已同步更新 `MEMORY.md` §6 与技能 `aioa-e2e-regression` §5。

## 收口补齐（同日二次提交）
- 发现首轮 `c0cde37` 遗漏了 3 处**已跟踪**的收尾改动（`scripts/` 只有 `e2e_*.py` 被 gitignore，
  老脚本如 `h5_v33_render.py` 是**跟踪文件**，改了不提交会留脏工作区）：
  `scripts/h5_v33_render.py`(+162 行 A2-6/A3-8 断言)、`scripts/repro_logout_401.py`(登录表单补租户名)、
  `.workbuddy/memory/{2026-09-16,MEMORY}.md`。
- 二次提交 `6fdc8dd chore(收口): 二期三期回归套件与记忆归档`，推送成功 `c0cde37..6fdc8dd main -> main`；
  校验本地 `6fdc8ddd11ea2ad1d0ffbae972c97c7bb52244e1` == 远端 `refs/heads/main`（一致），工作区已无跟踪脏文件。
- 教训：**收口前必须 `git status --porcelain | grep -v '^??'` 确认无跟踪脏文件**，
  不能假定「scripts 全被 ignore」——只有 `e2e_*.py` 被忽略。

---

# 2026-09-16 · docs/28「未开工项」全量收口（第四期/第五期 + D-1~D-8 + G-1~G-3）

## 完成范围
- **F4/F5/F3-1**（四期策略族 `DEPT_DUTY`/`UNIT_DUTY` + 五期 `mode` single/parallel/grab + `when` 条件路由 + `cc` 指定人）
  → 迁移 `V45__approver_resolver_and_node_modes.sql`；包 `org/support/approver/`（`ApproverStrategy`/`ApproverResolver`/`DutyApproverStrategy` 及三实现/`ApproverStrategyConfig`）。
- **F3-2** 知会覆盖补齐 → `V47` 对四类 bizType 的**机构级**定义补 `cc`（V44 只做了租户级）。
- **F2-1** 部门名义申请对**本部门成员**只读可见。
- **D-1** 服务端令牌吊销 → `V46__token_revocation.sql`（`revoked_token`）+ JWT 带 `jti` + 过滤器验签/过期后再查吊销表。
- **D-2** `MethodArgumentTypeMismatchException` → 400（原本落 `Exception` 兜底 500）。
- **D-3** H5 `/approvals?scope=todo` 改为**按角色前置闸门**（不再无条件打）。
- **D-4** 管理端与 H5 **两端内联 SVG favicon**（不再探测 `/favicon.ico`）。
- **D-8** 新租户播种 → `LeaveTypeProvisioner`（监听 `TenantProvisionedEvent`，仅对**零 leave_type** 租户播种 6 类标准假种，常量与 tenant 2 逐字节一致）。
- **G-1** dept 40（tenant 3 医政科）补负责人；零成员部门自动跳过。
- **G-3** `org_member.duty_code` 由 27/116 → **116/116**（`V47` 回填；**不触碰** `job_title` 含「负责人」的行，保 A1/A2 零差异）。

## 判定「有效数据，不改」（附依据）
- **D-5/6/7**：tenant 4 是教育局演示租户，`V33ANNUAL*` 假种被 17 条余额 + 35 条申请**引用**，删除会让既有单据失去类型定义；tenant 2 的 6 条越权部门已软删（=审计留痕）。→ docs/20 的「残留」判断在此过宽。
- **G-2**：tenant 9 重复昵称经 `seed_multi_tenant.py` 溯源 = **同一自然人兼多角色**（同一 sys_user），改昵称会与 docs/14 账号清单冲突。

## 计划外发现并修复（3 项，均不在原清单）
1. **`decide()` 知会分支不可达**：CC 任务 `status='CC'`，而代码**先判 `PENDING` 后判 `task_role`** → 专门的「知会无需审批」提示永不可达，用户看到误导性的「该审批节点已处理（CC）」。修法：**先判 CC 角色再判状态**。
2. **H5 `activateTodoTab` 从未定义**：`gotoTodo()`/`showLeaveResultCard` 调用它 → 每次「返回待办」抛 `ReferenceError`。已补定义；并新增 `scripts/_refaudit_h5.js`（悬空函数引用哨兵）防复发。
3. **#245 管理端配置入口缺失**：引擎支持职务型/会签/条件路由/指定知会人，但配置页只能配 类型+等级+阈值+角色知会 → **能力事实上不可用**。重写 `ApprovalFlowConfigView.vue`（节点级四组控件 + 与后端同源的本地校验 + `_extra` 无损往返），常量集中到 `constants/permissions.ts` 唯一入口。

## 验收
- 新增套件：`e2e_v45_approver_modes` **44/44**、`e2e_v45_misc_fixes` **27/27**、`e2e_v45_config_ui` **25/25**（API 往返 + 保存期严格性 + 浏览器渲染 + **无损往返**）、`_refaudit_h5.js`。
- `e2e_full_system`：**152 → 155/155**，已知缺口 **0**、观察项 **0**。三处**陈旧说明/分桶**已清理：
  S1-15「登出无吊销」与 S5-13「类型不匹配兜底 500」由 `kchk`（已知缺口桶）**提升为硬 `chk`**；favicon 观察项改为真实断言 **S12-10**。
- 全量回归全绿零回归；`pnpm typecheck`（web/apps/shell）退出码 0。
- 收口记录写入 `docs/28` §6（落地物清单 / 分项结果 / 计划外修复 / 「有效数据不改」依据）。

## 教训
- **套件里的 `note()`/`kchk()` 会过期**：修复了根因却不清理，会让报告继续输出与事实相反的说明（本例一次清掉 3 条）。收口时必须把「已知缺口分桶」逐条复核，能提升为硬断言的就提升。
- 修改**真实租户级流程定义**做验收时，`finally` 必须按**原字节**还原（先登记 `(id, 原 steps_json)` 再改），不要用「内置兜底版本」猜着还原。
