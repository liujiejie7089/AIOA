# 2026-09-09

## V1.2 原型改造：用户端重做 + 数据下沉管理端

### 交付
- 后端：V10 迁移（5 表 + 种子）、5 Entity + 5 Mapper、6 Controller（Kpi/AdminKpi/Worker/AdminWorker/Result/AdminResult）。
- 管理端：`KpiView.vue`、`WorkersView.vue`、`ResultsView.vue` + 路由 + 菜单 + `resource.ts` 三组 admin API。
- 用户端：`user-client/index.html` 完全重写（~1500 行）——经营看板、数字员工、成果沉淀、会话内技能参数卡、一句话创建数字员工。
- 视觉：frontend-design 定调「工业实用 Industrial」——IBM Plex Sans/Mono、tabular-nums、品牌蓝 #2f6fed + 琥珀 #f59e0b、27 个 SVG symbol 图标全替代 emoji、氛围层 + 错峰入场动效。

### 测试（四组共 75 项全绿）
- 后端接口 28/28（含 T26–T28 普通用户访问 admin → 403 越权验证）
- 用户端浏览器 e2e 20/20（U01–U20，含真实 SSE 会话、无 JS 运行时错误）
- 管理端页面 e2e 7/7（经营数据表 10 行、数字员工 3 行、成果 2 行）
- 既有全链路回归 20/20（T00–T18，含 deepseek 真实推理、支付幂等、审批卡点、额度准入）

### 修复的真实 bug
1. `notifications` 返回 `{items,unread}` 非数组 → `.slice()` TypeError 中断 renderAll → 整页区块空白。加 `asArray()` + renderAll 逐块 try/catch。
2. `#page-chat` flush 布局未避让 76px Tab 栏 → 输入框被遮挡。加 `padding-bottom:76px`。
3. Flyway 版本号冲突（新迁移误用 V8，已存在）→ 重命名 V10。

### 提交
- commit `662f598` feat(V1.2) → 已推送 `github.com/liujiejie7089/AIOA` main（远端 HEAD 与本地一致）。
- `.gitignore` 追加 `.workbuddy` 临时产物（截图/调试脚本/文档抽取）忽略规则。

## Bug 修复 + 管理端三功能补全（commit e9f512d，已推送 GitHub main）

### 四个 Bug
1. 发起审批 500 → 根因 serve.py 代理缺 `do_PUT`（Python 501 根本没到后端）；补 do_PUT/do_PATCH。
2. 待办详情 → 新详情页 + 通过/驳回；`ApprovalService.syncResultStatus` 按 result_id 回写成果（通过→APPROVED/驳回→DRAFT）；V11 迁移加 `approval_order.result_id`。
3. AI 回显问题 → 双因：UI 默认 `model:'echo'` 改 'default'；RunService.subscribe 对已终结 run 重复推理落库 → 加 replay() 回放 + finish() 插入查重。
4. 消耗行重复 → stream 的 run.completed 分支去掉二次 onUsage。

### 管理端三功能
- 权限：`RoleResolver` SPI + `DbRoleResolver`，JwtAuthenticationFilter 每请求实时查角色（改角色即时生效）；PUT users/{id}/roles|status。
- 功能：V12 `app_registry.visible_scope`(ALL/ADMIN)；/admin/apps CRUD + /api/v1/apps 角色过滤。
- 模型：V13 `model_config` 表 + CRUD/设默认；保存即 JDK HttpClient POST agent `/internal/v1/models/apply` 热加载（`apply_overrides`）；V11–V13 迁移已验证。

### 本轮新发现并修复
- AdminController 构造器注入 appService/appRegistryMapper 丢失、AppRegistry getter/setter 丢失（Edit 落盘丢失，grep 验证后重补）。
- `setDefault` 不存在模型抛 IllegalArgumentException→500，改 BizException.notFound→404。
- **AdminView onMounted 漏调 loadModels()** → 模型卡片初始空态（UI 走查截图发现）。
- **index.html:1467 发起审批丢第 6 参 resultId**（修复被覆盖）→ 补回，否则审批结果不回写成果状态。
- 注意：`ApprovalView` 视图不含 resultId 字段，列表匹配审批单要用 title 不能用 resultId。

### 验证（全绿）
- 模型 API 15/15、功能管理 9/9、模型路由 e2e 7/7（切 echo 默认→回显；恢复 deepseek→真实回答）、审批链路回归 9/9、管理端 UI 走查 8/8（截图 logs/admin-page-full.png）。

### 推送
- commit `e9f512d` → github main 成功（需 `-c http.schannelCheckRevoke=false`；sslVerify=false 才能过公司网络 SSL 拦截）。
- **origin（172.16.8.249:3000 Gitea）403**：仓库 AIOA_System 不存在且禁止 push-to-create，需用户在 Gitea 建仓库或修权限。

### 运维备忘
- Maven：java 直接启动 Launcher（mvn 脚本在含空格路径失败），必须带 `-Dmaven.multiModuleProjectDirectory`。
- 打包前必须停 8080 后端（Windows jar 锁）；启动后查 Flyway 迁移日志。
- playwright：NODE_PATH 指向 `$LOCALAPPDATA/npm-cache/_npx/e41f203b7505f1fb/node_modules`，executablePath 用 chromium_headless_shell-1234。
- 端口：后端 8080（task x05eIH）、agent 8000（task I69lpA）、用户端 5181、管理端 vite 5173。
