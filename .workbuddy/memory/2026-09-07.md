# 2026-09-07 工作日志

## AIOA 智能办公基座 · M1 骨架完成

### 背景
用户委托设计并实现 AI OA 办公系统基座（AIOA），三部分：Vue3 前端 + Java 后端 + Python agent。目标让票务/统一调度等业务系统统一接入后自动获得 AI agent 能力。交接包（左老师设计）为蓝图借鉴。

### 本次完成
1. **澄清问题阶段**：向用户提了 4 组澄清问题（接入方式/agent 栈/AI 形态/模型来源 + 调用通道/数据边界/执行管控/多agent + 认证/文件上传/部署/系统现状 + 示例系统/模型优先级/UI 库），确认全部关键决策。
2. **方案设计**：规划代理产出详细实现方案，写入 `.workbuddy/plans/quantum-aurora-tesla-eBlzDTfe.md`。
3. **文档骨架**：docs/ 下 6 份文档（架构设计/子应用接入规范/工具注册规范/接口契约×2/部署手册/交接包借鉴说明）。
4. **部署件**：deploy/ 下 docker-compose、nginx、3 个 Dockerfile、.env.example、postgres initdb。
5. **Python agent**：FastAPI + 回声 SSE 模式，pytest 3 通过（由子代理完成）。
6. **Java 后端**：Maven 多模块（common/security/admin/bridge/chat/tool-sdk/boot），编译+打包通过，产出 aioa-boot-0.1.0-SNAPSHOT.jar。
7. **前端**：pnpm workspace（shell 主应用 + demo-ticket/demo-dispatch 子应用 + aioa-sdk），全部 build 通过。

### 关键决策记录（用户确认）
- 微前端 wujie（子代理）/ 前端嵌入优先
- 中间层工具注册表模式：业务系统注册工具，agent 经中间层鉴权调用
- 基座不存业务数据，只存账号/会话/配置/知识库索引/审计，预留 tenant_id
- 主 agent 编排式多 agent 协同 + HITL 高风险操作人工确认
- 统一模型网关双模式（云端 API / 私有化 vLLM·Ollama）
- JWT 自建账号体系 + 预留 OAuth2/LDAP
- Docker Compose 起步，预留 K8s

### 技术要点/踩坑（重要）
- **本机系统 mvn 损坏**：使用 `.tools/mvnw.sh`（自建启动器，经 bash -x 排查是 classpath 问题）代替 mvn/mvn.cmd。
- **Maven 目录是 `.tools/apache-maven-3.9.9`**，启动器 `.tools/mvnw.sh` 用绝对路径调 java + plexus classworlds。
- **两套代码并存合并**：子代理和我并行写导致重复类。最终以子代理为主体（Lombok 风格），删除我写的半成品（common/api、security 子包 config/filter/jwt/user、admin/dto 孤儿）。
- **父 pom 缺 Lombok**：子代理 28 个文件用 Lombok 注解但 pom 没引，已补全局 provided 依赖。
- **BizException 缺静态工厂**：补 unauthorized/forbidden/notFound/badRequest/notImplemented。
- **common 缺 mybatis 依赖**（AbstractJsonbTypeHandler 用 org.apache.ibatis.type），补 org.mybatis:mybatis 3.5.16。
- **GlobalExceptionHandler 依赖 spring-security**：从 common 移除 security 异常处理（401/403 由 security 模块 entryPoint 处理），保持 common 轻量无循环依赖。
- **bridge/chat 缺 mybatis-plus 依赖**：已补。
- **aioa-tool-sdk 模块漏加进父 pom modules**：已补。

### 待办（M2 起）
- M2：工具注册表完整实现 + 主/子 agent 编排 + 真实模型接入 + mock 业务系统
- M3：RBAC 完整 + HITL 审批 + 审计哈希链
- M4：知识库 RAG + 附件解析 + 模型双模式验证 + 开放 API
