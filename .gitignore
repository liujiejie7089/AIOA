# ---- OS / IDE ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.iml

# ---- Node / Web ----
node_modules/
dist/
dist-ssr/
dist-verify/
*.local
.npm/
.pnpm-store/

# ---- Java ----
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# ---- Env / Secrets（密钥一律不入库）----
.env
.env.*
!.env.example
# 两套环境模板：只含占位值（CHANGE_ME__/DEV_ONLY__），显式入库便于对照与切换。
# 真实密钥请写 `.env`（上面已忽略）或部署平台 Secret，禁止回填进这两个模板。
!deploy/.env.development
!deploy/.env.production

# ---- Local tools / data ----
.tools/
logs/
*.log
data/

# ---- Docs temp ----
*.tmp

# ---- AIOA 本地临时产物 / 密钥（一律不入库）----
.aioa_login.json
.diag.txt
.docx_extract/
.workbuddy/_check.js
.workbuddy/_uc.js
.workbuddy/sse_*.txt
.workbuddy/verify_*.py
.workbuddy/verify_*.sh
.workbuddy/*.log
.workbuddy/proxy*.log

# ---- E2E / 浏览器调试产物 ----
ab_*.png
ab_*.txt
ab_*.html
e2e_*.json
e2e_*.py
e2e_*.sh
/extract_docx.py

# ---- 一次性诊断脚本与其快照（验完即弃，不入库）----
# 注意：`_check_*.py` 是**长期回归探针**（如 _check_gitea_ui_provider.py），
# 不在此列 —— 它们要入库，否则真机接线的界面/数据哨兵就会丢。
scripts/_diag_*
scripts/_tmp_*
scripts/_q.py
scripts/_*.png

# ---- 本地后端启动 / 冒烟脚本产物（不入库）----
boot-run*.log
boot-run*.err
start-backend.bat
.proxy.pid
.backend.pid
.smoke_*.py
.aioa_check.js
proxy-run*.log

# ---- WorkBuddy 工作区临时产物（截图/调试/文档抽取，不入库）----
.workbuddy/*.png
.workbuddy/*.txt
.workbuddy/doc_*.json
.workbuddy/p0_*.json
.workbuddy/p0_*.txt
.workbuddy/debug_*.py
.workbuddy/probe_*.py
.workbuddy/restore_*.py
.workbuddy/_*.js

# ---- 体验走查截图：walkthrough 原图已 base64 内嵌进 用户体验走查报告.html，不入库 ----
ux-review/*.png
# 批次改造证据截图体积小且未内嵌，保留入库
!ux-review/batch1/*.png
# 历史审计轮次的多角色整页截图（admin/dsj_admin/fagai_admin/jyj_admin × 各页面）：
# 既未内嵌进报告、也无任何脚本/文档引用，属可重新生成的过程产物 —— 不入库。
# 需要时重跑：python ux-review/walkthrough.py（截图目录用 WALKTHROUGH_OUT 指定）。
ux-review/full-audit/*.png
ux-review/admin-audit/*.png
# 保留这两轮的审计数据（报告渲染与角色菜单矩阵的真源，体积小）
!ux-review/full-audit/report.json
!ux-review/admin-audit/report.json
!ux-review/admin-audit/menu-by-role.json

# ---- 根目录临时探测脚本 ----
/_db_inspect.py
/_trigger_run.py
/_reconstruct_history.py
/flyway_history_backup_*.json
# 演示数据清理前的行快照（本地回滚用，不入库）
/cleanup_backup_*.json
# 演示环境额度复位前的行快照（本地回滚用，不入库）
/quota_backup_*.json

# ---- 运行时上传产物（知识库/审批附件，属本地数据不入库）----
/uploads/
/server/uploads/

# ---- 角色矩阵 / 权限验证截图（验完即弃，不入库）----
/shot-*.png

# ---- 走查基线图与改造后对照图（原图已 base64 内嵌进 用户体验走查报告.html）----
ux-review/before-fix/
ux-review/after-fix/
