# =============================================================================
# Java 后端（aioa-boot）
#
# 两个要点：
#  1. Flyway 迁移在启动时执行，首次冷启动可能超过 60s —— readinessProbe 必须给足
#     initialDelaySeconds，否则会被反复重启、迁移跑到一半。
#  2. 有状态的部分只有 uploads（挂载 PVC）；会话与业务真值都在 MySQL/Redis，
#     因此**可以水平扩容**，但回填类迁移要按单副本发布。
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aioa-server
  namespace: aioa
  labels:
    app: aioa-server
spec:
  replicas: 1
  # 迁移期间禁止并行：滚动会同时起两个实例抢同一把 Flyway 锁
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: aioa-server
  template:
    metadata:
      labels:
        app: aioa-server
    spec:
      containers:
        - name: server
          image: aioa/server:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: aioa-config
            - secretRef:
                name: aioa-secrets
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://$(MYSQL_HOST):$(MYSQL_PORT)/$(MYSQL_DB)?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: aioa-config
                  key: MYSQL_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: aioa-secrets
                  key: MYSQL_PASSWORD
            - name: AIOA_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: aioa-secrets
                  key: JWT_SECRET
            - name: AIOA_SERVICE_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: aioa-secrets
                  key: SERVICE_JWT_SECRET
            - name: MINIO_ACCESS_KEY
              valueFrom:
                configMapKeyRef:
                  name: aioa-config
                  key: MINIO_ROOT_USER
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: aioa-secrets
                  key: MINIO_ROOT_PASSWORD
          resources:
            requests: { cpu: "1", memory: "2Gi" }
            limits:   { cpu: "2", memory: "4Gi" }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8080 }
            initialDelaySeconds: 60      # 首次迁移可能很慢
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8080 }
            initialDelaySeconds: 180
            periodSeconds: 20
            failureThreshold: 6
          volumeMounts:
            - name: uploads
              mountPath: /app/uploads
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: aioa-uploads
---
apiVersion: v1
kind: Service
metadata:
  name: aioa-server
  namespace: aioa
spec:
  selector:
    app: aioa-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
