# AIOA 用户端配置模板：复制为 .env 后按需修改
# 优先级：命令行参数 python serve.py [port] > 本文件 > 代码默认值
# PORT          用户端联调服务监听端口（静态托管 + /api 反代）
# BACKEND_HOST  Java 后端地址
# BACKEND_PORT  Java 后端端口（aioa-boot 默认 8080）
PORT=5181
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8080
