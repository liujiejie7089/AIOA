# AIOA 用户端配置模板：复制为 .env 后按需修改
# 优先级：命令行参数 python serve.py [port] > 本文件 > 代码默认值
# HOST          监听地址。默认 127.0.0.1（仅本机）。**放进容器必须改 0.0.0.0**，
#               否则同网络的 nginx / 宿主都连不上（容器内 127.0.0.1 是容器自己）
# PORT          用户端联调服务监听端口（静态托管 + /api 反代）
# BACKEND_HOST  Java 后端地址。容器内跑时改成 compose 服务名 `aioa-server`
#               （服务名解析走 docker 内网 DNS；写 127.0.0.1 会指向自己）
# BACKEND_PORT  Java 后端端口（aioa-boot 默认 8080）
HOST=127.0.0.1
PORT=5181
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8080
